void p2(){
  strokeWeight(5);
  stroke(white);
  float[][] nums = new float[40][40];
  for(int i = 0; i < nums.length; i++){
    for(int j = 0; i <nums.length;j++){
      
    float temp = (float)(Math.random()*255);
    fill(temp);
    square(i,j,20);
    }
  }
  for(int i = 0; i < nums.length; i++){

    
    
   
  }
  strokeWeight(1);
    stroke(0);
    fill(1);
}
